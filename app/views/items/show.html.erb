<div class = "container">
      <div class="d-flex justify-content-center mt-5">
        <div class="w-50">
          <%= image_tag @item.photos.attached? ? cl_image_path(@item.photos[0].key) : "doggreen.jpg", class: "w-100" %>
          <div class="main-text">
            <div class="d-flex">
              <p class="info-pill">Size: <%= @item.size %></p>
              <p class="info-pill mx-3">Rating: <%= @item.rating %>/5</p>
            </div>
            <h4 class="my-3"> £<%= @item.price.to_i  %>/day</h4>
            <p><%= @item.description %></p>
            <p>Owner:    <%= @item.user.first_name %> <%= image_tag @item.user.photo.attached? ? cl_image_path(@item.user.photo.key) : "https://kitt.lewagon.com/placeholder/users/ssaunier", class:"avatar" %></p>
          </div>

          <div class="d-flex">
          <% if @item.user_id == current_user.id %>
            <%= link_to "Delete the sWag", item_path(@item), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} , class: "btn btn-gradient btn-lg"%>
          <% else %>
            <%= link_to "Rent #{@item.name}" , new_item_booking_path(@item), class: "btn btn-gradient btn-lg" %>
          <% end %>
          </div>
        </div>
        <%# <div id="carousel-whole">
          <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
              <div class="carousel-indicators">
                <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
                <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="2" aria-label="Slide 3"></button>
              </div>
            <div class="carousel-inner">
              <div class="carousel-item active">
                <%= cl_image_tag @item.photos[0].key, class:"d-block w-100" %>
              <%# </div> %>
              <%# <div class="carousel-item"> %>
                <%# <%= cl_image_tag @item.photos[1].key, class:"d-block w-100" %>
              <%# </div> %>
              <%# <div class="carousel-item"> %>
                    <%# <%= cl_image_tag @item.photos[2].key, class:"d-block w-100" %>
              <%# </div> %>
            <%# </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div> %>
        <%# </div> %>
        <div class="booking-form-sm w-50" id="black-text">
          <div class="details p-3 mx-5">
            <h2><%= @item.name%></h2>
            <%= render 'bookings/booking' %>
          </div>
        </div>
      </div>
      <div class="my-5">
        <h2>More <%= @item.category %>s</h2>
        <%= render "all" %>
      </div>
</div>
