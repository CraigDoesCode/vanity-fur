

<div class = "container">
  <%= link_to "Back to all items", items_path %>
  <div class=""><%= @item.name  %></h1>
  <div class="d-flex justify-content-center">
    <div class="w-50">
      <%= cl_image_tag @item.photos[0].key, class: "w-100" %>
      <h3><%= @item.name%></h3>
      <div class="main-text">
        <div class="d-flex">
          <p>Size: <%= @item.size  %> </p>
          <p>Rating: <%= @item.rating %></p>
        </div>
        <p>More info: <%= @item.description %></p>
        <p>Owned by:    <%= @item.user.first_name %> <%= image_tag @item.user.photo.attached? ? cl_image_path(@item.user.photo.key) : "https://kitt.lewagon.com/placeholder/users/ssaunier", class:"avatar" %></p>
      </div>
      <div class="d-flex">
        <%= link_to "Rent this item", new_item_booking_path(@item), class: "btn btn-gradient btn-lg" %>
        <h4> £<%= @item.price.to_i  %>/day</h4>
      </div>
    </div>
      <%# <div id="carousel-whole">
        <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-indicators">
              <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
              <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
              <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="2" aria-label="Slide 3"></button>
            </div>
          <div class="carousel-inner">
            <div class="carousel-item active">
              <%= cl_image_tag @item.photos[0].key, class:"d-block w-100" %>
            <%# </div> %>
            <%# <div class="carousel-item"> %>
              <%# <%= cl_image_tag @item.photos[1].key, class:"d-block w-100" %>
            <%# </div> %>
            <%# <div class="carousel-item"> %>
                  <%# <%= cl_image_tag @item.photos[2].key, class:"d-block w-100" %>
            <%# </div> %>
          <%# </div>
          <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
          </button>
        </div> %>
      <%# </div> %>

    <div class="details p-3 mx-5">
      <%= render 'bookings/booking' %>
    </div>
  </div>
</div>
